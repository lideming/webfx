{
  "name": "@yuuza/webfx",
  "version": "1.9.11",
  "description": "",
  "repository": "github:lideming/webfx",
  "main": "dist/webfx.js",
  "module": "dist/webfx.esm.js",
  "types": "build/index.d.ts",
  "browser": "dist/webfx.min.js",
  "scripts": {
    "build": "pnpm run build:i18n && pnpm run build:webfx",
    "build:webfx": "tsc && rollup -c",
    "build:i18n": "cd packages/i18n && pnpm run build",
    "watch": "tsc && rollup -c -w",
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepack": "pnpm run build",
    "buildpages": "cp -R dist demo/"
  },
  "files": [
    "dist/",
    "build/**/*.d.ts",
    "style.css"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "rollup": "^2.66.1",
    "rollup-plugin-sourcemaps": "^0.6.3",
    "rollup-plugin-terser": "^7.0.2",
    "typescript": "^4.5.5"
  },
  "dependencies": {
    "@yuuza/i18n": "workspace:^1.2.1"
  }
}
